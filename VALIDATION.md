# ผลตรวจเว็บ Verdant

## สถานะล่าสุด

- เว็บสะท้อนอุปกรณ์จริงตามตารางใน `Code_Board/README.md` เท่านั้น (ESP32 38pin, จอ ESP32-2432S028, PCA9685, Servo MG996R × 4, JGB37-520 + L298N, ปุ่มเขียว × 4, E-Stop, Load Cell + HX711, XL4016, Adapter 12V)
- Node tests: ผ่าน 14/14 (`npm test`) ครอบคลุม netlist 49 สาย, เลข GPIO ตรงกับ firmware, ไม่มีอุปกรณ์นอกตาราง และ `CALIBRATION_OFFSET_G` เป็น 0 ทั้งหมด
- Production build: ผ่านด้วย Vite
- ตรวจด้วยเบราว์เซอร์แล้ว: เปิดหน้าเดินสาย 3D, คลิกที่ ESP32 แล้วเปิดตัวแก้โค้ด, แก้/บันทึก/คืนค่า/สลับบอร์ด/ไปที่ค่า Calibrate ทำงาน, เปิดโค้ดจากรายละเอียดอุปกรณ์ได้ และไม่มี console error

## ข้อจำกัด

- เป็นแบบจำลองและเอกสารอ้างอิง ไม่ได้เชื่อมต่อฮาร์ดแวร์จริง
- ตัวแก้โค้ดเก็บการแก้ไขในเบราว์เซอร์เครื่องนั้น (localStorage) ไม่มี backend จึงไม่อัปโหลดเข้าบอร์ดเอง
- เลขขาของ ESP32 ในฉาก 3D อ้างตามผัง DevKitC V4 ต้องตรวจตำแหน่งขาบนบอร์ด 38 ขาจริงจากชื่อ GPIO ก่อนต่อ
- HawkScan DAST ไม่ได้รัน: เว็บเป็น static ไม่มี backend และไม่ได้ตั้ง `HAWK_API_KEY`
- ผังใน `wokwi/` ยังเป็นรุ่นเก่า ไม่ตรงกับ netlist ปัจจุบัน
- ยังไม่ได้ตรวจภาพละเอียดของฉาก 3D ในหน้าจอขนาดต่าง ๆ (ดูผ่านหน้าต่าง preview ขนาดเดียว)
